<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.resumecraft</groupId>
        <artifactId>resume-craft-parent</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>resume-craft-server</artifactId>
    <name>resume-craft-server</name>
    <description>启动模块：聚合所有领域模块 + 启动类 + 配置（微服务化后由网关/各服务替代）</description>

    <dependencies>
        <!-- 聚合全部领域模块 -->
        <dependency>
            <groupId>com.resumecraft</groupId>
            <artifactId>resumecraft-common</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.resumecraft</groupId>
            <artifactId>resumecraft-auth</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.resumecraft</groupId>
            <artifactId>resumecraft-resume</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.resumecraft</groupId>
            <artifactId>resumecraft-job-match</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.resumecraft</groupId>
            <artifactId>resumecraft-application</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- 运行时基础设施（应用级） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
