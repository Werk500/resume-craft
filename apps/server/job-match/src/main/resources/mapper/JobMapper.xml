<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.resumecraft.server.job.domain.JobMapper">

    <insert id="insertIgnoreBatch">
        INSERT IGNORE INTO job
        (company, title, department, location, salary_range, description,
        requirements, source_url)
        VALUES
        <foreach collection="list" item="j" separator=",">
            (#{j.company}, #{j.title}, #{j.department}, #{j.location}, #{j.salaryRange},
            #{j.description}, #{j.requirements}, #{j.sourceUrl})
        </foreach>
    </insert>

</mapper>