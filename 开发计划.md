# AI简历设计与优化软件 — 开发实施计划

**关联需求**：《需求文档.docx》V1.1（持续完善中）  
**模式**：分里程碑逐步交付，每个里程碑结束产出「可运行、可验证」的增量，与用户确认后再进入下一阶段。

---

## 0. 目标与范围

- 短期目标（MVP）：走通「简历上传 → AI诊断 → 一键优化」核心链路。
- 中期目标（V1.0）：打通「岗位搜索 → JD解析 → 匹配度 → 定向优化 → 版本管理/投递」闭环。
- 长期目标（V1.5+/V2.0）：扩展公司覆盖、对话式创建、模板市场等。
- 需求文档作为「活文档」，每个里程碑结束后同步更新（补充实现决策、接口定义、偏差说明）。

---

## 1. 技术栈与工具链（已按本机环境确认）

| 层 | 选型 | 本机状态 |
|:---|:---|:---|
| 前端 | Next.js 14 + TypeScript + Tailwind CSS + shadcn/ui + Zustand + Framer Motion + Recharts | Node 24 / npm 11 ✅ |
| 后端 | Java 17 + Spring Boot 3.2 + Spring AI + Spring Security/JWT | JDK 17 + Maven 3.9 ✅ |
| 数据 | PostgreSQL + pgvector、Redis | Docker Compose（引擎当前未启动，M4 前接入） |
| 文件 | MinIO（S3 兼容对象存储） | Docker Compose（MVP 先用本地文件存储） |
| 包管理 | npm（前端）、Maven（后端） | ✅（pnpm 沙箱受限，用 npm） |

> 说明：RabbitMQ、MongoDB、Elasticsearch、Prometheus/Grafana 等中间件延后到对应里程碑再引入，MVP 不依赖。
>
> 环境适配：Maven 本地仓库指向 workspace 内 `.m2-repo`（见 `apps/server/settings-dev.xml`，阿里云镜像）；npm 使用 `registry.npmmirror.com` 镜像；均因本机对官方源/全局仓库访问受限。

---

## 2. 关键架构决策（已确认）

| 决策点 | 定案 | 理由 |
|:---|:---|:---|
| AI 接入方式 | 国产大模型，默认 DeepSeek（OpenAI 兼容，可换智谱/通义）；开发期先 Mock/规则跑通，获得 Key 后切换真实模型 | 无前期 API 成本、可离线开发与测试、兼容协议保证多模型无缝切换 |
| 后端起步形态 | 模块化单体（按领域分包）起步，V1.0+ 再拆分微服务 | 显著降低 MVP 复杂度与启动成本，包边界即未来服务边界 |
| AI 框架 | Spring AI（统一 ChatClient 抽象） | 与文档一致，天然支持模型可替换 |
| MVP 中间件 | 开发期默认 H2 + 本地文件 + 内存缓存，零 Docker 依赖；PostgreSQL(pgvector)/Redis/MinIO 于 M4 前接入 | 当前 Docker 引擎未启动；MVP（上传/诊断/优化）链路无需重型中间件，先快速跑通 |

---

## 3. 里程碑规划

### M0 — 工程脚手架与环境（首个目标）
**产出**：monorepo 结构 + 前端骨架 + 后端骨架 + 中间件 compose，全部可启动验证。
- 初始化 monorepo（`apps/web` + `apps/server`）与 Git 仓库
- 前端：Next.js + TS + Tailwind + shadcn/ui 骨架，跑通首页
- 后端：Spring Boot 3.2 骨架 + 健康检查接口 `/actuator/health`
- `docker-compose.yml`：PostgreSQL(pgvector) + Redis + MinIO
- `README.md` 开发指引 + 统一代码规范

**进度**：✅ 已完成
- [x] monorepo 结构 + Git 仓库 + README/.gitignore
- [x] 后端骨架：Maven 构建通过；`/` 与 `/actuator/health` 返回 UP（H2 连接正常）
- [x] `docker-compose.yml`（PostgreSQL+pgvector / Redis / MinIO）已编写
- [x] 前端骨架：依赖安装完成（npmmirror 镜像）；`npm run dev` 启动成功，http://localhost:3000 返回 200
- [x] Docker 引擎：本机 Docker Desktop 未就绪，**不阻塞 MVP**，M4 前解决

**验收**：`npm run dev` 前端可访问；后端启动并返回健康状态；`docker-compose.yml` 已提供（引擎就绪后 `docker compose up -d` 即可拉起中间件，MVP 由 H2 兜底）。

### M1 — 简历上传与解析
**产出**：上传 PDF/Word/图片 → 结构化简历 JSON。
- 文件上传接口（本地存储起步，M4 后切 MinIO）
- 解析服务：PDF（PDFBox）、Word（poi/docx 解析）、图片（Tesseract OCR + 兜底）
- 前端上传页 + 解析结果预览
**验收**：三种格式上传均能解析出结构化字段（姓名/联系方式/经历等）。

### M2 — AI 诊断
**产出**：上传简历 → 生成可视化诊断报告。
- 诊断服务：基础信息检查（规则）+ 内容质量评分（STAR 维度）+ 评分公式
- AI 抽象层 + Mock/规则评分实现（预留真实模型）
- 前端诊断报告页（总分、维度雷达图、红黄绿标注、修改建议，`< 5s`）
**验收**：输入简历 → 得到含总分/各维度得分/建议的报告。

### M3 — 一键优化
**产出**：诊断后一键优化 + 前后对比。
- 优化服务：整份改写（保留事实）+ 逐句精修入口
- 前端优化对比页（分屏/高亮 + 「AI优化说明」）
**验收**：一键优化 → 查看改写版本与差异说明。

### M4 — 岗位搜索 + JD 解析 + 匹配度
**产出**：选择岗位 → 计算并展示匹配度。
- 岗位数据：初期用种子样例 JD（合规、无爬虫），后续接爬虫
- JD 智能解析（硬性要求/加分项/隐性要求 + 雷达图）
- pgvector 向量化 + 匹配度计算（按 3.4.4 公式）；本阶段接入 PostgreSQL+pgvector/Redis/MinIO
**验收**：选岗位 → 展示各维度要求 + 简历匹配度（可解释归因）。

### M5 — 定向优化 + 版本管理 + 账号
**产出**：闭环走通「岗位→匹配→定向优化→导出→记录投递」。
- 账号体系：注册/登录/JWT（简历、版本、投递绑定用户）
- 定向优化（关键词植入/经历重排/缺口提示 + 匹配度提升对比）
- 简历版本管理 + 投递记录追踪
- 导出 PDF/Word
**验收**：完整闭环可跑通，多版本可管理、可对比、可记录投递。

### M6 — 工程化交付与文档收口
- 单元测试 + 集成测试 + GitHub Actions CI
- docker-compose 一键起全套（含后端/前端生产构建）
- 监控（Prometheus/Grafana，可选）
- **完善需求文档**：记录所有实现决策、接口定义、技术偏差与演进

---

## 4. 需求文档持续完善机制

- 每个里程碑结束：更新 `需求文档.md` 的「迭代规划」勾选进度，新增/修订对应章节（接口、数据模型、实现决策）。
- 出现与需求不符的技术取舍时，在需求文档相关章节追加「实现说明/偏差记录」。
- 用 Git 提交历史记录文档演进，保留 `需求文档_原始备份.docx` 作为基线。

---

## 5. 开发约定

- 代码与文档均 UTF-8；提交信息用中文 + 语义化前缀（`feat`/`fix`/`docs`）。
- 后端按领域分包（`resume`、`diagnose`、`optimize`、`job`、`auth`…），包即未来微服务边界。
- AI 相关逻辑一律依赖抽象接口，先落 Mock/规则实现。
- 每个里程碑提交可运行版本，附快速启动说明。